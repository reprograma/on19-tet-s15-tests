const teste = (tarefa, esperado, retornado) => {
    if (esperado === retornado){
        console.log(`${tarefa} deu certo :)`);
     } else {
            console.log(`${tarefa} deu ruim ):`)
        }
    }